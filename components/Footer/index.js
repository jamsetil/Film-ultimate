import React from "react";
import styles from "./styles.module.css";

const Footer = () => {
  return (
    <footer className={styles.foot}>
      <small>Made by jamsetil</small>
    </footer>
  );
};

export default Footer;
